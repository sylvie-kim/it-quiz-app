# 159개 전체 경로 배치 실험 보고서

## 실험 조건

이 문서는 모든 용어를 하나 이상의 학습 경로에 넣는 A안이 어떤 준비 부족을 드러내는지 기록한다. 경로 동시 배치는 의미 관계가 아니다. 공개 관계는 콘텐츠 책임자가 근거·이유·검토 기록을 확인한 `REVIEWED` 상태만 허용한다.

| 항목 | 기준선 | A안 | 근거 |
|---|---:|---:|---|
| 전체 용어 수 | 159 | 159 | 두 감사 JSON의 `summary.entities` |
| 학습 주제 수 | - | 12 | `ontology-data.js` |
| 학습 경로 수 | 0 | 15 | `ontology-data.js` |
| 강제 조건 | 적용 전 | 159개 모두 하나 이상의 경로에 포함 | 경로 역색인과 무결성 검사 |
| 실제 배치 수 | 0 | 175 | 159개 중 16개가 두 경로에 배치 |
| 공개 의미 관계 | 0 | 0 | `relations` 및 `REVIEWED` 관계 수 |

기준선 감사는 경로를 넣기 전 commit `1cf49265aeb07d2210b8ea528a1b364ff78e654a`의 입력 해시를 보존한다. A안 감사는 commit `a586a8a78216a0ea569c4a237a7c2f10d8c994d6`의 입력 해시를 보존한다. 배치 후 데이터를 기준선으로 다시 만들지 않았다.

## 강제 배치 전후 비교

| 분류 | 개수 | 결함 코드 | 대표 refId |
|---|---:|---|---|
| 배치 전후 지속 | 163 | `ENTITY_TYPE_AMBIGUOUS` 159건, `COMPETENCY_QUESTION_UNANSWERED` 4건 | `term-api`, `cq-1` |
| 사전 작업으로 해소 | 1 | `COMPETENCY_QUESTION_UNANSWERED` 1건 | `cq-4` |
| 강제 배치로 새로 관찰 | 3 | `PATH_ASSIGNMENT_WEAK` 3건 | `path-ai-coding-tools:term-playwright` |
| 원인 미귀속 상태로 잔존 | 0 | 없음 | 없음 |
| 분류되지 않음 | 0 | 없음 | 없음 |

`cq-4`는 159개 용어가 배치 근거와 함께 유효한 경로에 포함되어 학습 순서를 반환할 수 있게 되어 해소됐다. 반대로 `cq-1`, `cq-2`, `cq-3`, `cq-5`는 검토 완료 관계가 없으므로 각각 선행 학습, 개념 비교, 함께 쓰는 도구·실행 환경, 연결 근거를 답할 수 없다.

## 관찰 결과

| 문제 유형 | 개수 | 대표 사례 | 사용자 영향 |
|---|---:|---|---|
| 개체 유형 미판별 | 159 | `term-api`를 concept·service·tool 등으로 구분할 `termKind` 없음 | 같은 라벨이라도 어떤 속성과 관계가 허용되는지 자동으로 판단할 수 없음 |
| 약한 경로 배치 | 3 | TDD를 `런타임과 명령어`, Selenium·Playwright를 `AI 코딩 도구`에 배치 | 전부 보이게 하려는 조건이 학습 목적보다 앞서면 잘못된 학습 묶음이 생김 |
| 다중 경로 배치 | 2개 이상 경로에 놓인 용어 16개, 총 175개 배치 | HTML, CSS, JavaScript, React, GitHub 등은 정확히 두 경로에 배치 | 반복 자체는 허용되지만, 재사용 목적과 우선순위를 기록하지 않으면 같은 용어가 왜 여러 번 나오는지 알기 어려움 |
| 과다 반복 배치 진단 | 0 | 품질 진단의 경고 기준은 4개 이상 경로(`> 3`) | 경고가 0이라는 뜻은 2개 이상 경로의 반복 16개가 없다는 뜻이 아님 |
| 검토 완료 관계 없는 경로 | 15/15 | 모든 학습 경로 | 화면은 경로 순서를 보여줄 수 있지만, 용어 간 사실 관계를 선으로 주장할 수 없음 |
| 중복 라벨 후보 | 0 | 없음 | 안정 ID·별칭 정규화 단계에서는 충돌이 관찰되지 않음 |
| 속성 결함 | 0 | 없음 | 정의·별칭·등록부 ID의 현 입력은 감사 규칙을 통과함 |
| 관계 제약 오류 | 0 | 없음 | 관계 레코드가 0건이므로, 관계 품질이 검증됐다는 뜻은 아님 |
| 출처 부족 관계 | 0 | 없음 | 검토 완료 관계가 0건이므로, 출처 승격 절차의 실증도 아직 없음 |

Task 7의 경로별 객관식 생성에서도 사전 작업이 하나 더 드러났다. 경로 안의 정답만 쓰되, 오답은 정답 정의에 이미 노출되지 않아야 하고 네 개의 고유 선택지가 필요하다. 수동 혼동군만으로는 일부 경로에서 오답 세 개를 만들 수 없었다. 현재 구현은 **수동 혼동군 → 같은 온톨로지 주제 → 기존 카테고리** 순서로 후보를 보충하고, 정의에 등장한 후보는 제외한다. 이는 퀴즈 선택지용 후보 규칙이지 의미 관계를 새로 승인한 것이 아니다. 다음 데이터셋에서는 `혼동군`, `학습 주제`, `객관식 오답 후보`를 별도 속성·검토 대상으로 관리해야 한다.

## 원인과 필요한 사전 작업

| 관찰 | 원인 가설 | 먼저 정의해야 할 것 | 재검증 방법 |
|---|---|---|---|
| 159개 모두 `termKind` 미판별 | 용어명·정의·주제만 있고 실제 개체 종류 판정 기준이 없음 | `concept`, `method`, `process`, `language`, `framework`, `service`, `tool`, `command`, `file`, `format`, `platform`, `event`의 판별 기준과 각 용어의 값 | 모든 등록부 레코드에 유효한 `termKind`를 넣고 `ENTITY_TYPE_AMBIGUOUS`가 0인지 감사 |
| TDD, Selenium, Playwright 약한 배치 | 독립 QA·테스트 학습 목적이 없는 상태에서 100% 배치를 강제함 | 경로 목표, primary/supporting 기준, 허용 적합도, 약한 배치 승인·보류 규칙 | 테스트·브라우저 자동화 경로를 설계하거나 약한 배치를 보류한 뒤 `PATH_ASSIGNMENT_WEAK` 비교 |
| 16개 용어가 두 경로에 반복 | 경로 간 재사용 목적과 다중성 규칙이 명시되지 않음 | `appears_in_path`의 허용 횟수, 반복 이유, 주 경로/보조 경로 규칙 | 경로별 역할을 기록하고 반복 수·이유·학습 순서가 일치하는지 검사 |
| 12개 주제 분류의 의미 검토 미완료 | 현재 주제는 경로 탐색과 후보 선별을 위한 구조 분류이며, 용어별 주제 판단 기준과 콘텐츠 책임자 검토 기록이 없음 | 주제 taxonomy, 용어별 분류 기준, 다중 주제 허용 규칙, content-owner 검토·변경 기록 | 모든 용어의 주제 배정에 기준·검토 기록을 붙이고 분류 일관성 표본 검토와 재감사를 실행 |
| 15개 경로에 공개 관계 없음 | 사람 검토 기록과 근거가 없는 상태에서 관계를 제품에 넣지 않았음 | 관계 사전의 domain/range/방향/대칭성/다중성/순환·금지 조합, 관계별 최소 근거, `content-owner` 승격 절차 | 후보 관계를 검토한 뒤 유효한 `REVIEWED` 관계만 넣고 관계 검증·출처·화면 표시를 함께 검사 |
| `cq-1`, `cq-2`, `cq-3`, `cq-5` 미응답 | 선행·비교·함께 사용·근거 조회에 필요한 검토 관계가 없음 | 각 competency question에 필요한 관계 유형과 답변 최소 증거 | 관계와 출처를 넣은 뒤 질문별 경로·이유·출처가 반환되는지 자동 검사 |
| 경로 객관식 오답 부족 가능성 | 용어의 학습 주제는 있으나 혼동군과 오답 후보의 의도가 데이터화되지 않음 | `confusionGroup`, 오답 후보의 학습 목적, 정의 노출 제외 규칙, 후보 검토 기준 | 각 경로의 모든 용어에서 네 개의 고유 선택지가 생성되고 정의가 정답을 누설하지 않는지 검사 |

## 온톨로지 준비도 결론

| 사전 작업 | 상태 | 이번 실험의 증거 | 다음 데이터셋에 재사용할 규칙 |
|---|---|---|---|
| 정본 데이터와 안정 ID | 부분 충족 | 159개, 중복 후보 0 | 원본 라벨과 불변 ID를 분리하고 별칭 충돌은 등록 시 중단 |
| 주제 분류 | 구조 충족·의미 검토 미완료 | 12개 주제, 159개 모두 경로 배치. 다만 TDD, Selenium, Playwright의 약한 배치 3건은 주제·경로 의미가 아직 콘텐츠 검토로 확정되지 않았음을 보여줌 | 주제 소속은 탐색·후보 선별용이며 의미 관계로 표시하지 않음. taxonomy·분류 기준·content-owner 검토를 별도 관리 |
| 개체 유형 | 미충족 | `ENTITY_TYPE_AMBIGUOUS` 159건 | 유형 값과 판별 기준을 업무 책임자가 먼저 확정 |
| 속성 사전 | 부분 충족 | 현 정의·별칭·등록부 ID 결함 0 | 필수·허용값·결측 처리 규칙을 데이터 계약으로 검사 |
| 관계 사전과 제약 | 설계만 충족 | 관계 제약 오류 0, 공개 관계 0 | 오류 0을 관계 품질 증명으로 해석하지 말고, 승인된 실제 관계로 검증 |
| 근거·검토·변경 이력 | 미충족 | `REVIEWED` 관계 0, 모든 경로가 관계 근거 없음 | 관계마다 출처·연결 이유·검토 역할·기록 ID·날짜를 보존 |
| 학습 경로 | 부분 충족 | `cq-4` 해소, 약한 배치 3건 | 100% 배치는 관찰 조건이며, weak는 숨기지 않고 재설계 대상으로 남김 |
| 퀴즈 오답 설계 | 부분 충족 | 경로별 객관식 15개 모두 생성 가능 | 혼동군·주제·카테고리 보충은 의미 관계와 분리하고, 정의 노출을 자동 차단 |

## 그래프가 답해야 할 질문

| competency question | 답변 가능 여부 | 사용한 관계·경로 | 부족한 사전 정의 |
|---|---|---|---|
| cq-1: 무엇을 먼저 배워야 하는가 | 아니오 | `prerequisite_of` 검토 관계 0건 | 선행 판정 기준·근거·검토 |
| cq-2: 무엇과 비교하면 차이가 선명한가 | 아니오 | `contrasts_with` 검토 관계 0건 | 비교 기준·양쪽 정의 근거 |
| cq-3: 무엇을 함께 쓰거나 어디서 실행하는가 | 아니오 | `used_with`·`runs_on` 검토 관계 0건 | 관계 경계·방향·근거 |
| cq-4: 목표별 학습 순서는 무엇인가 | 예 | 15개 학습 경로, 159개 배치 | 경로 목표와 적합도 기준은 계속 검토 필요 |
| cq-5: 이 연결의 근거는 무엇인가 | 아니오 | 출처가 있는 검토 관계 0건 | 출처·검토 기록 승격 절차 |

## 다른 도메인에 재사용할 사전 작업 순서

IT Quiz의 용어·관계·결과를 의료·운영 데이터에 복사하지 않는다. 아래 순서만 재사용하고, 각 제품에서 업무 책임자·PHI 경계·승인 규칙을 별도로 정한다.

1. competency question 정의
2. 정본 데이터와 개체 경계 확정
3. 안정 ID·정본 라벨·별칭 규칙 확정
4. 개체 유형과 유형 판별 기준 확정
5. 속성 사전과 결측·허용값 규칙 확정
6. 관계 사전(domain/range/방향/대칭성/다중성/순환/금지 조합) 확정
7. 허용 근거·검토 역할·변경 이력 확정
8. 후보 추출 후 인간 검토
9. 무결성·질문 응답 가능성·사용성 검증
10. 승인된 관계만 제품에 노출

## 배포 판단

| 구분 | 상태 | 근거 |
|---|---|---|
| A안 로컬 데이터·경로 구현 | 조건부 완료 | 159/159 등록·배치, 온톨로지 검증 오류 0, 경로 초과 0 |
| 의미 관계 그래프 | needs content review | `REVIEWED` 관계 0, 15/15 경로에 공개 관계 근거 없음 |
| 콘텐츠 준비도 | needs content review | 개체 유형 미판별 159건, 약한 경로 배치 3건, competency question 4개 미응답 |
| 브라우저 QA | 통과 | 별도 승인된 `58de3ee`의 지연 검색기 조회 수정 후, 실제 용어·정의 키워드 검색과 경로 퀴즈 전환을 세 화면 크기에서 재검증 |
| 공개 배포 | 수행하지 않음 | 이번 범위는 로컬 A안 실험이며, push·PR·병합·배포는 별도 승인 대상 |

### 브라우저 QA 실측

로컬 정적 서버(`127.0.0.1:4173`)에서 실제 브라우저로 확인했다. 아래 증거는 Playwright 접근성 스냅샷, viewport DOM 측정(`clientWidth`, `scrollWidth`), console 조회 결과다. 이 검증 산출물은 비추적 파일이며 커밋하지 않는다.

| 화면 | 크기 | 확인 결과 | 상태 |
|---|---:|---|---|
| 데스크톱 | 1512×982 | 학습 지도 진입, `전체 용어 159` 선택 시 12개 주제·159개 용어 표시, `scrollWidth === clientWidth === 1512`, console error 0 | 통과 |
| 검색·상세·CTA | 1512×982 | `API` 검색 → `API(Application Programming Interface)` 선택 → `API로 시스템 연결하기` 경로와 상세 표시 → CTA로 범위 퀴즈 `1 / 7` 진입 | 통과 |
| 정의 키워드 검색 | 1512×982 | `접점` 검색 → `Interface(인터페이스)` 결과 2개 중 선택 → 선택 경로와 Interface 정의 상세 표시 | 통과 |
| 태블릿 세로 | 834×1194 | 지도·경로·상세가 표시되고 `scrollWidth === clientWidth === 834`, 데스크톱 흐름 `grid`, 모바일 단계 목록 `none` | 통과 |
| 모바일 | 390×844 | `scrollWidth === clientWidth === 390`, 데스크톱 흐름 `none`, 모바일 단계 목록 `block` | 통과 |
| 키보드 | 데스크톱·모바일 | 지도 검색창에서 Escape로 입력·결과 초기화; 객관식 정답 선택 뒤 Enter로 `1 / 15`에서 `2 / 15`로 이동 | 통과 |
| 기존 화면 | 모바일 | 홈, 용어사전, 사전의 `접점` 검색 결과, 객관식 첫 문제를 실제 브라우저에서 확인 | 통과 |
| 결과 화면 | 1512×982 | `API로 시스템 연결하기` 7문제 경로를 완료하고 `결과 보기`로 진입. `퀴즈 완료`, `0 / 7`, 오답 복습 7개 표시, `scrollWidth === clientWidth === 1512`, console error 0 | 통과 |
| 결과 화면 | 834×1194 | 같은 7문제 경로를 다시 완료. `퀴즈 완료`, `0 / 7`, 오답 복습 7개 표시, `scrollWidth === clientWidth === 834`, console error 0 | 통과 |
| 결과 화면 | 390×844 | 같은 7문제 경로를 다시 완료. `퀴즈 완료`, `7 / 7`, `100%` 표시, `scrollWidth === clientWidth === 390`, console error 0 | 통과 |

초기 재현했던 검색 실패는 `ontology-utils.js`가 `quiz-content.js`보다 먼저 평가될 때 아직 없는 검색기를 저장하던 로드 순서 문제였다. 별도 승인된 수정 commit `58de3ee`는 검색 시점에 현재 검색기를 조회하도록 바꿨다. 이번 재검증에서 `API`와 정의 키워드 `접점` 모두 실제 결과를 반환했고, 지도 검색 결과에서 선택한 용어의 상세와 경로 범위 퀴즈까지 이어졌다. 이 보고서의 Task 8 커밋에는 그 별도 코드 수정 파일을 포함하지 않는다.
