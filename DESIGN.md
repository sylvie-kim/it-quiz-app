# IT Quiz 디자인 기준

## 제품과 사용자

IT Quiz는 바이브코딩을 시작하는 사람이 기본 용어를 구분하고, 틀린 이유를 바로 이해하는 학습 도구다. 사용자는 긴 설명을 정독하기보다 정의와 선택지를 빠르게 훑고 답한다.

디자인의 성공 기준은 다음과 같다.

- 사용자가 현재 문제, 진행률, 다음 행동을 한눈에 찾는다.
- 선택형은 답을 고르는 한 번의 행동으로 채점된다.
- 틀린 경우 어떤 용어와 혼동했는지 설명한다.
- 390px부터 데스크톱까지 가로 스크롤이 생기지 않는다.
- 긴 해설 중에도 `다음 문제` 또는 `결과 보기` 버튼이 보인다.

## 시각 방향

스타일은 장식적인 학습 앱이 아니라 차분한 편집형 도구에 가깝게 유지한다.

- 기본 테마는 다크 모드다. 라이트 모드는 사용자가 선택한다.
- 강조색은 진행률, 핵심 상태, 주 행동에만 쓴다.
- 같은 위계의 항목은 그림자 카드가 아닌 선으로 구분한 플랫 리스트로 표시한다.
- 반복되는 이니셜 타일, 이모지, 그라데이션, 유리 효과, 컬러 왼쪽 테두리를 사용하지 않는다.
- 3D 비주얼은 홈의 학습 맥락을 보강하는 한 개만 사용한다. 문제와 결과 화면에는 장식 이미지를 넣지 않는다.

## 정보 위계

한 화면에서 굵은 글씨와 강조색을 동시에 쓰는 정보는 한 단계에 집중한다.

1. 문제 화면: 정의 또는 판단할 용어와 정의
2. 채점 후: 정답 여부
3. 보조 정보: 문제 유형, 안내, 진행률, 해설 라벨

문구는 결과와 행동을 구체적으로 말한다. `시작하기`, `더 알아보기` 같은 추상적 표현과 근거 없는 최상급 표현은 사용하지 않는다.

## 퀴즈 흐름

- 객관식과 참/거짓은 선택 즉시 채점한다.
- 단답형과 상황 적용만 `답 확인`을 사용한다.
- 채점 전 행동 영역에는 짧은 조작 안내를 표시한다.
- 채점 후 같은 위치에 `다음 문제` 또는 `결과 보기`를 표시한다.
- 해설 아래에 공간이 있으면 행동 영역을 본문에 바로 이어 붙인다.
- 내용이 길어 행동 영역이 화면 밖으로 밀릴 때만 뷰포트 하단 안쪽에 고정하고, 본문에는 겹침 방지 여백을 둔다.

## 참/거짓 해설

- 참이 정답이고 사용자가 맞힌 경우: `정답입니다`만 표시한다.
- 거짓이 정답인 경우: 제시된 용어의 실제 정의와 문제에 나온 정의의 실제 용어를 함께 표시한다.
- 오답인 경우: 사용자의 판단과 정답을 밝히고 혼동한 정의를 비교한다.
- 900px 이하에서는 비교 영역을 한 열로 바꾼다.

## 결과 화면

- 정답 수와 정답률을 텍스트 위계로 보여준다. 원형 점수, 그래디언트, 축하 이모지를 사용하지 않는다.
- 오답은 독립된 형제 목록으로 표시하고 카드를 중첩하지 않는다.
- 각 항목에는 문제, 내 답, 정답, 실제 정의만 남긴다.

## 학습 지도

- 전체 주제 지도는 탐색용이고, 목적별 학습 경로는 실제 학습용이다. 둘을 같은 의미의 선으로 섞지 않는다.
- 공개되는 모든 선은 관계명, 사용자가 읽을 수 있는 연결 이유, 확인 가능한 출처를 함께 가진다.
- 단순한 주제 소속이나 같은 경로 안의 배치는 의미 관계로 그리지 않는다.
- 모바일에서는 복잡한 그래프 대신 읽을 수 있는 단계 목록을 우선한다.
- 지도 화면의 주 행동은 선택한 경로를 바로 연습하는 `이 경로로 퀴즈 풀기` 하나다.

## 반응형 기준

- 콘텐츠 최대 너비: 전체 960px, 퀴즈와 결과 820px
- 중간 화면: 900px 이하에서 긴 비교 설명을 한 열로 표시
- 모바일: 720px 이하에서 홈 비주얼, 결과 요약, 사전 항목을 한 열로 표시
- 최소 지원 너비: 390px 검증, CSS 최소 너비 320px
- 모든 주요 화면에서 `documentElement.scrollWidth`가 뷰포트 너비를 넘지 않아야 한다.

## 금지 목록

- 동일한 둥근 카드의 반복
- 박스 안의 박스
- 장식용 왼쪽 컬러 선
- 의미 없는 브랜드 이니셜 타일
- 로봇, 뇌, 마법봉, 반짝이처럼 상투적인 AI 이미지
- 설명보다 강한 그림자와 광택
- 다음 행동을 찾기 위한 불필요한 스크롤
